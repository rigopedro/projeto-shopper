
export const isMeasureDuplicate = (costumer_code: string, measure_datetime: string, measure_type: string): boolean => {
    return false; 
  };
  
  export const isMeasuredConfirmed = (measure_uuid: string): boolean => {
    return false; 
  };
  